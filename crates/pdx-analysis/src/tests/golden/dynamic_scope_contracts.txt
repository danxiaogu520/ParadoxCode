// case: dynamic_scope_contracts
// input:
//   clash = { add_prestige = 1 add_province_modifier = { name = clash_mod duration = 1 } }
//   via_callee = { helper_province = yes add_prestige = 1 }
//   helper_province = { change_province_name = "X" }
//   fine = { add_prestige = 1 add_manpower = 1000 }
//   dual_ok = { add_prestige = 1 add_core = FRA }
//   scoped_ok = { any_country = { change_province_name = "Y" } }
//   nested_ok = { if = { limit = { always = yes } add_prestige = 1 } }
//   dynamic_dispatch = { $action$ = yes }
//   root_opaque = { add_prestige = 1 ROOT = { change_province_name = "Z" } }
//   this_opaque = { add_prestige = 1 THIS = { change_province_name = "W" } }
//   or_union = { if = { limit = { OR = { is_capital = yes has_estate_privilege = some_priv } } add_prestige = 1 } }
//   or_open = { if = { limit = { OR = { unknown_branch_key = yes is_capital = yes } } add_prestige = 1 } }
//   
diagnostics:
- code=EmptyScopeContract severity=Error certainty=certain range=0..5
  message: dynamic definition `clash` has an empty inferred entry scope: no scope satisfies every statement in its body
- code=InvalidValue severity=Error certainty=certain range=60..69
  message: invalid value `clash_mod` for `name`
  expected: an `event_modifier` name, or a `static_modifier` name
- code=EmptyScopeContract severity=Error certainty=certain range=87..97
  message: dynamic definition `via_callee` has an empty inferred entry scope: no scope satisfies every statement in its body
- code=InvalidValue severity=Warning certainty=certain range=233..237
  message: invalid value `1000` for `add_manpower`
  expected: a number between -999 and 999
- code=InvalidValue severity=Error certainty=certain range=280..283
  message: invalid target `FRA`: scope expression is not recognised
  expected: a `country` scope, one of `name`, `path`, `start_from_root`, or a `province` scope, or 1 more constraints
- code=UnknownKey severity=Error certainty=certain range=300..311
  message: `any_country` is a trigger and cannot be used inside an effect block; conditions belong in a `limit` block
- code=ConstantCondition severity=Warning certainty=certain range=361..363
  message: `limit` of this `if` is always true; the branch wrapper is redundant
- code=InvalidValue severity=Error certainty=certain range=675..684
  message: invalid value `some_priv` for `has_estate_privilege`
  expected: an `estate_privilege` name
- code=UnknownKey severity=Error certainty=certain range=746..764
  message: unknown key `unknown_branch_key` in a `trigger` block
