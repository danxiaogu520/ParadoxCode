# tree-sitter-localisation

The Tree-sitter grammar for Paradox localisation files. It is a separate
frontend from Script because localisation has language headers, line-oriented
entries, and quoted text with Paradox substitutions.

Run the corpus and parser checks with:

```text
npm install
npm test
```
