# Cross-crate tests

This directory is reserved for original fixtures and integration tests that exercise more than
one core crate. It must not contain Vanilla EU4 files or copied reference-project corpora.

