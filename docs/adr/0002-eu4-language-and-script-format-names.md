# EU4 language and script format names

Zed exposes EU4 key/value files as `Europa Universalis IV` with the machine identifier `eu4`,
while reusable parser and rule types use `Script`. This keeps the EU4-first editor experience
explicit without making the generic syntax, rules, workspace, or analysis layers game-specific.
