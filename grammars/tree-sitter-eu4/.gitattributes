# Generated by `tree-sitter generate`, which always writes LF. Pin these files to LF so a
# regeneration stays byte-identical to the index even when core.autocrlf converts other text
# files to CRLF on checkout; otherwise every hook run reports them as modified.
src/** text eol=lf
